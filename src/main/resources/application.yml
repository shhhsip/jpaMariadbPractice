spring:
  profiles:
    active: local
  datasource:
    url: jdbc:mariadb://localhost:3306/tuning2
    driver-class-name: org.mariadb.jdbc.Driver
    username: root
    password: 751004

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        #format_sql: true
        #use_sql_comments: true

#  data:
#    web:
#      pageable:
#        default-page-size: 10
#        max-page-size: 2000

logging.level:
  org.hibernate.SQL: debug
# org.hibernate.type: trace

